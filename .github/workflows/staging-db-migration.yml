name: Staging DB Migration

on:
  pull_request:
    types:
      - closed
    branches:
      - main
    paths:
      - 'supabase/migrations/**'

concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.base.ref }}
  cancel-in-progress: false

jobs:
  migrate:
    if: github.event.pull_request.merged == true
    runs-on: ["self-hosted", "linux", "superoptimizers", "staging-db-migration"]
    permissions:
      id-token: write
    steps:
      - name: Request OIDC token
        id: oidc
        run: |
          TOKEN=$(curl -sS -H "Authorization: bearer $ACTIONS_ID_TOKEN_REQUEST_TOKEN" \
            "${ACTIONS_ID_TOKEN_REQUEST_URL}&audience=api.superoptimizers.cofounder.co" | jq -r '.value')
          echo "token=$TOKEN" >> $GITHUB_OUTPUT

      - name: Run migrations
        env:
          OIDC_TOKEN: ${{ steps.oidc.outputs.token }}
        run: |
          curl -sS -f -X POST https://api.superoptimizers.cofounder.co/migrations/v1/run \
            -H "Authorization: Bearer $OIDC_TOKEN"
